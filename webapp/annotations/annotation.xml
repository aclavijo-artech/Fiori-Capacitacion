<!--
  Este archivo define anotaciones OData (EDMX) que extienden el metadata original del servicio Northwind.
  Las anotaciones permiten personalizar la visualización y comportamiento en aplicaciones Fiori Elements.
-->

<edmx:Edmx xmlns:edmx="http://docs.oasis-open.org/odata/ns/edmx" Version="4.0">

  <!-- Importa vocabulario común de SAP (etiquetas, definiciones generales) -->
  <edmx:Reference Uri="https://sap.github.io/odata-vocabularies/vocabularies/Common.xml">
    <edmx:Include Namespace="com.sap.vocabularies.Common.v1" Alias="Common"/>
  </edmx:Reference>

  <!-- Importa el vocabulario de UI para configurar vistas, encabezados, filtros, etc. -->
  <edmx:Reference Uri="https://sap.github.io/odata-vocabularies/vocabularies/UI.xml">
    <edmx:Include Namespace="com.sap.vocabularies.UI.v1" Alias="UI"/>
  </edmx:Reference>

  <!-- Importa vocabulario relacionado a información de contacto y comunicación -->
  <edmx:Reference Uri="https://sap.github.io/odata-vocabularies/vocabularies/Communication.xml">
    <edmx:Include Namespace="com.sap.vocabularies.Communication.v1" Alias="Communication"/>
  </edmx:Reference>

  <!-- Referencia al metadata original del servicio OData de Northwind -->
  <edmx:Reference Uri="/V2/Northwind/Northwind.svc/$metadata">
    <edmx:Include Namespace="NorthwindModel"/>
    <edmx:Include Namespace="ODataWeb.Northwind.Model"/>
  </edmx:Reference>

  <!-- Sección donde se definen las anotaciones personalizadas -->
  <edmx:DataServices>
    <Schema xmlns="http://docs.oasis-open.org/odata/ns/edm" Namespace="local">      
    </Schema>
  </edmx:DataServices>
</edmx:Edmx>
