_schema-version: "3.2"
ID: comandresclase7
description: Generated by Fiori Tools
version: 0.0.3

modules:
- name: comandresclase7-destination-content
  type: com.sap.application.content
  requires:
  - name: comandresclase7-destination-service
    parameters:
      content-target: true

  - name: comandresclase7-repo-host
    parameters:
      service-key:
        name: comandresclase7-repo-host-key

  - name: comandresclase7-uaa
    parameters:
      service-key:
        name: comandresclase7-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comandresclase7_html_repo_host
          ServiceInstanceName: comandresclase7-html5-service
          ServiceKeyName: comandresclase7-repo-host-key
          sap.cloud.service: comandresclase7

        - Authentication: OAuth2UserTokenExchange
          Name: comandresclase7_uaa
          ServiceInstanceName: comandresclase7-xsuaa-service
          ServiceKeyName: comandresclase7-uaa-key
          sap.cloud.service: comandresclase7
        existing_destinations_policy: update
  build-parameters:
    no-source: true

- name: comandresclase7-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comandresclase7-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comandresclase7.zip
      name: comandresclase7
      target-path: resources/

- name: comandresclase7
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: comandresclase7-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: comandresclase7-destination-service
    service-plan: lite

- name: comandresclase7-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comandresclase7-xsuaa-service
    service-plan: application

- name: comandresclase7-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comandresclase7-html5-service
    service-plan: app-host

parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true